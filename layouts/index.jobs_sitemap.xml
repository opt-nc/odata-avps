<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{ range .Site.RegularPages }}
    {{/* 🎯 On prend n'importe quelle page du site du moment qu'elle possède une référence d'AVP et qu'elle n'est pas clôturée */}}
    {{ if and .Params.ref (ne .Params.actif false) }}
    <url>
      <loc>{{ .Permalink }}</loc>
      <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>
      <changefreq>daily</changefreq>
      <priority>0.8</priority>
    </url>
    {{ end }}
  {{ end }}
</urlset>